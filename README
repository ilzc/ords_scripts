ORDS Scripts
=====
Some helper scripts that can do help to inscribe.

sendmany.py
A helper script that distribute satoshis to `ord` generated addresses.Making more UTXOs to implement batch inscribing. Make sure `ord` is installed and added to system environment $PATH.

`bitcoin_data_dir` - bitcoin data dir
`wallet_name` - wallet name 
`sats` - the amount of satoshi to distribute to.
`count` - total addresses to generate.

usage:
``` 
python sendmany.py <bitcoin_data_dir> <wallet_name> <sats> <count>
```
sample: 
```
python sendmany.py e:\\Bitcoin ord 1000 5
```


Code generated by ChatGPT-4.